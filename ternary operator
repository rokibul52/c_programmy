#include<stdio.h>
int main()
{  int num1,num2;
   printf("Enter a number:");
   scanf("%d%d",&num1,&num2);
   (num1>num2) ? printf("%d is bigger than %d",num1,num2):printf("%d is smaller than %d",num1,num2);
   return 0;
}
