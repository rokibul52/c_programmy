#include<stdio.h>
int main()
{
    int n, a=1,sum=0;
    printf("Enter the last number of the series:");
    scanf("%d",&n);
    while (a<=n)
    {
     sum=sum+a;
     a=a+1;
    }
    printf("1+3+5+.....%d = %d",n,sum);

}
