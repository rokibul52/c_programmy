#include<stdio.h>
int main()
{
    int num[]={50,10,5,463,5,54};
    int size=sizeof(num)/sizeof(num[0]);
    printf("%d is the size of array.",size);
    for(int i=0; i<size; i++)
    {
        for(int j=i+1; j<size; j++)
        {
            if(num[i]==num[j])
            printf("\n%d is the duplicate number. ",num[j]);
        }
    }
    
}
