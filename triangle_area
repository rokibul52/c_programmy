#include<stdio.h>
int main(){
    float base,height,area;
    printf("Enter base and height:");
    scanf("%f%f",&base,&height);
    area=(base*height)/2;//type casting method:(float)1/2*base*height
    printf("The area of the triangle is:%.2f\n",area);
   

    return 0;
}
